public class xXORxHW2 {
    //What is the value of x^x for any valueof x?
    public static void main(String[] args) {
        //The value of x^x = 0.
        //Think about it,xor gives 0 when the bits are the same.
        //If we compare the same number to itself, the bits will always be the same. 
        //So, the answer of x^x will always be 0
        System.out.println(5^5);
        
    }
}
